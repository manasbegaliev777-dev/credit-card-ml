# A/B Test Plan: RandomForest vs LogisticRegression

## Цель

Определить, превосходит ли LogisticRegression (model_v2) RandomForest (model_v1) по качеству кредитного скоринга в производственных условиях при сохранении приемлемого уровня одобрений.

---

## Группы теста

| | Группа A (контроль) | Группа B (эксперимент) |
|---|---|---|
| **Модель** | `model_v1.pkl` — RandomForestClassifier | `model_v2.pkl` — LogisticRegression |
| **Параметры** | n_estimators=100, random_state=42 | max_iter=1000, StandardScaler |
| **Метрики офлайн** | F1=0.4572, ROC-AUC=0.7506 | F1=0.3553, ROC-AUC=0.7076 |

---

## Разделение трафика

**Метод:** детерминированное хэширование по `user_id`

```python
import hashlib

def get_group(user_id: str) -> str:
    hash_val = int(hashlib.md5(user_id.encode()).hexdigest(), 16)
    return "A" if hash_val % 2 == 0 else "B"
```

- **50% трафика** → Группа A (model_v1)
- **50% трафика** → Группа B (model_v2)

**Преимущества подхода:**
- Один и тот же `user_id` всегда попадает в одну группу (стабильность)
- Не требует хранения состояния
- Равномерное распределение гарантировано свойствами MD5

---

## Метрики оценки

### Первичные метрики
| Метрика | Описание | Целевое направление |
|---|---|---|
| **F1-score** | Баланс Precision/Recall на реальных данных | Выше у группы B |
| **Precision** | Точность предсказания дефолта | Выше у группы B |
| **Снижение дефолтов** | % дефолтов в одобренных заявках | Ниже у группы B |

### Вторичные метрики (guardrail)
| Метрика | Ограничение |
|---|---|
| Approval Rate | Не упасть ниже 70% в группе B |
| P99 latency API | Не превысить 200 мс |
| Error rate | Не превысить 0.1% |

---

## Статистический метод

**Метод:** Z-test для двух независимых долей

**Гипотезы:**
- H₀: F1(B) − F1(A) ≤ 0 (модель B не лучше A)
- H₁: F1(B) − F1(A) > 0.05 (модель B лучше на 5%+)

**Параметры теста:**
```
Уровень значимости:  α = 0.05
Мощность теста:      1 − β = 0.80
Минимальный эффект:  δ = 0.05 (5% улучшение F1)
```

**Расчёт минимального размера выборки:**
```
n = (Z_α + Z_β)² × (p₁(1−p₁) + p₂(1−p₂)) / δ²

Где:  Z_α = 1.645 (одностороннний тест)
      Z_β = 0.842
      p₁ ≈ 0.46 (базовый F1 группы A)
      δ = 0.05

Результат: n ≈ 2 500 наблюдений на группу
```

**Формула Z-статистики:**
```
Z = (p_B - p_A) / sqrt(p̄(1-p̄)(1/n_A + 1/n_B))

где p̄ = (n_A * p_A + n_B * p_B) / (n_A + n_B)
```

---

## Длительность теста

**Продолжительность:** 4 недели (28 дней)

**Обоснование:**
- Покрывает полный платёжный цикл (ежемесячные выплаты)
- Нивелирует сезонные и недельные паттерны поведения
- При объёме ~2 000 заявок/день → ~56 000 наблюдений >> минимум 2 500

**График:**

```
Неделя 1:  Запуск, мониторинг стабильности разделения (SRM-check)
Неделя 2:  Промежуточный анализ (не для принятия решений, только guardrail)
Неделя 3:  Накопление статистической мощности
Неделя 4:  Финальный сбор данных, подготовка к анализу
День 29:   Итоговый статистический анализ и решение
```

---

## Критерий успеха

**Модель B признаётся победителем если выполнены ВСЕ условия:**

1. F1(B) > F1(A) × 1.05 — улучшение F1 на 5% и более
2. p-value < 0.05 — статистически значимый результат
3. Approval Rate(B) ≥ 70% — бизнес-ограничение не нарушено
4. Latency P99(B) ≤ 200 мс — SLA не деградировал

**Действия по итогам:**

| Результат | Действие |
|---|---|
| B побеждает | Перевести 100% трафика на model_v2, обновить `model_v1.pkl` |
| A побеждает | Оставить model_v1, зафиксировать в MLflow, закрыть эксперимент |
| Нет разницы | Оставить model_v1 (меньший риск), исследовать feature engineering |
| Нарушен guardrail | Немедленно остановить тест, rollback на группу A |

---

## Мониторинг во время теста

```python
# Пример структуры лога для каждого предсказания
{
    "timestamp": "2024-01-15T10:23:45Z",
    "user_id": "u_12345",
    "group": "B",
    "model_version": "model_v2",
    "prediction": 0,
    "probability": 0.31,
    "latency_ms": 45,
    "actual_default": null  # заполняется через 30 дней
}
```

**SRM-check (Sample Ratio Mismatch):** на 3-й день проверить, что соотношение групп A:B = 50:50 ± 1% через χ²-тест. Отклонение сигнализирует о баге в роутинге.

---

## Доверительные интервалы

### Формула

```
CI = p ± Z × sqrt(p(1-p) / n)

где:
  p  — наблюдаемая доля (например, F1-score)
  n  — размер выборки
  Z  — квантиль нормального распределения (1.96 для 95% CI)
```

### Пример расчёта

Параметры: n = 2 500, p = 0.22 (доля дефолтов в группе), Z = 1.96

```
margin = 1.96 × sqrt(0.22 × 0.78 / 2500)
       = 1.96 × sqrt(0.0000686)
       = 1.96 × 0.00828
       = 0.01623

CI = 0.22 ± 0.0162
   = [0.2038, 0.2362]
```

**Интерпретация:** с вероятностью 95% истинная доля дефолтов в группе находится в диапазоне **20.4% — 23.6%**.

### Применение в тесте

| Группа | F1 | n | CI (95%) |
|---|---|---|---|
| A (model_v1) | 0.4572 | ~15 000 | [0.449, 0.465] |
| B (model_v2) | 0.3553 | ~15 000 | [0.348, 0.363] |

Если интервалы **не пересекаются** → разница статистически значима без формального теста.  
Если **пересекаются** → необходим Z-test для точного p-value.

---

## Риски и митигация

| Риск | Вероятность | Митигация |
|---|---|---|
| Новизна эффекта (novelty bias) | Низкая — скоринг не виден клиенту | Не применимо |
| Дрейф данных за 4 недели | Средняя | Мониторинг PSI еженедельно |
| Неравномерное распределение групп | Низкая | SRM-check на день 3 |
| Деградация latency у model_v2 | Низкая — LR быстрее RF | Мониторинг P99 |
